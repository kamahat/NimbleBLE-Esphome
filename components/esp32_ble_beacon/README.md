# components/esp32_ble_beacon — SURCHARGE de `esphome/components/esp32_ble_beacon`

**Statut : squelette, M1-M2.** Beacon (iBeacon/Eddystone) — dépend uniquement de
`components/esp32_ble` (advertising), pas de `ble_device_base`/GATT. Port relativement
mécanique une fois `esp32_ble` opérationnel.
